[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mendmark-evals"
version = "0.1.0"
description = "Reproducible evaluations for agents repairing broken machine-learning systems"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Daniel Gaskins" }]
dependencies = []

[project.scripts]
mendmark = "mendmark.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
