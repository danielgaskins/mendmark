{
  "schema_version": "1.0",
  "id": "metric-aggregation-001",
  "title": "Stop frequent tasks from dominating an agent score",
  "failure_class": "metric-design",
  "difficulty": "introductory",
  "description": "Repair evaluation aggregation so each task contributes equal weight and malformed trials cannot silently alter the score.",
  "checks": {
    "test_tasks_receive_equal_weight": "Each task contributes equal weight to the headline score",
    "test_duplicate_trial_is_rejected": "Duplicate trial identifiers are rejected",
    "test_empty_records_are_rejected": "An empty result set is rejected",
    "test_malformed_fields_are_rejected": "Malformed result fields are rejected"
  },
  "public_dir": "repo",
  "hidden_tests_dir": "hidden_tests",
  "grader": {
    "command": ["/usr/bin/python3", "-B", "-m", "unittest", "discover", "-s", "tests", "-p", "test_*.py", "-v"],
    "timeout_seconds": 30
  }
}
